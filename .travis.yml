language: java

sudo: required

notifications:
email: james.hn.sears@gmail.com

services:
  - docker

jdk:
  - oraclejdk8

cache:
  directories:
  - $HOME/.m2

script:
  - mvn clean test
  - mvn package -DskipTests

after_success:
  - mvn jacoco:report coveralls:report
